[Unit]
Description=duckdns service
After=network.target

[Service]
Type=simple
User=lazka
WorkingDirectory=~
ExecStart=/bin/bash --login -c 'cd quodlibet-continuous && ./run-duckdns.sh'
Restart=always

[Install]
WantedBy=multi-user.target
